.venv/
__pycache__/
*.pyc
rustbpe/target/
dev-ignore/
report.md
eval_bundle/

# TCT wheel files and build artifacts
*.whl
*wheel*.zip
# Exception: pre-built TCT wheels ARE committed
!tct-wheels/
!tct-wheels/*.whl
tct-github-workflow/
tct-github-workflow-py/
tct-github-workflow-wheel.zip
tct/

# Model checkpoints (large files, don't commit)
checkpoints/
checkpoints-bpe/
*.pt

# Training logs
logs/*.log
runpod_logs/

# Evaluation results (local only)
results/

# Local developer guide (keep local-only)
CLAUDE.md

# RunPod upload staging (large files)
runpod-upload/

# Archives (never commit, except training data)
*.tar.gz
*.tar
*.zip
# Exception: training data archives ARE committed
!data/*.tar.xz
checkpoints_analysis/
